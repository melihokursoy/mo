// Re-export all Phosphor icons from a dedicated module so consumers can import
// from `@codecrib/ui/icons` instead of pulling `phosphor-react` directly.
export * from 'phosphor-react';

export default undefined;
